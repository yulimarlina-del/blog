# My Personal Blog

A personal blog built with **React Router v7** (Remix v3) to explore the latest developer experience in building modern web applications.

## 🚀 About React Router v7 (Remix v3)

React Router v7 is the next evolution of Remix, combining the power of Remix with React Router in a unified package. It brings:

- **Full Vite integration** for faster build times and better DX
- **Server rendering** with React Server Components support
- **Type-safe routing** and data loading
- **Enhanced performance** optimizations
- **Unified package** (no more separate @remix-run packages)
- **Better developer experience** with improved error messages

## 📋 Prerequisites

- Node.js >= 20.0.0
- npm >= 10.0.0

## 🛠️ Setup

1. Clone the repository:
```bash
git clone https://github.com/yulimarlina-del/blog.git
cd blog
```

2. Install dependencies:
```bash
npm install
```

3. Start the development server:
```bash
npm run dev
```

4. Open your browser and visit:
```
http://localhost:5173
```

## 📦 Available Scripts

- `npm run dev` - Start the development server with hot module replacement
- `npm run build` - Build the application for production
- `npm run start` - Start the production server
- `npm run typecheck` - Run TypeScript type checking

## 🏗️ Project Structure

```
blog/
├── app/
│   ├── routes/
│   │   └── _index.tsx      # Homepage route
│   ├── root.tsx             # Root layout component
│   └── routes.ts            # Route configuration
├── vite.config.ts           # Vite configuration
├── react-router.config.ts   # React Router configuration
├── tsconfig.json            # TypeScript configuration
└── package.json             # Project dependencies
```

## 🎨 Features

- Clean, responsive design with Inter font
- Server-side rendering (SSR)
- Type-safe routing
- Fast development with Vite HMR
- Modern React patterns

## 📝 License

This project is for educational purposes to learn React Router v7 (Remix v3).

## 🤝 Contributing

Feel free to fork this project and experiment with React Router v7 features!

---

Built with ❤️ using React Router v7 (Remix v3)
